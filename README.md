# hyperfleet-credential-provider
Kubernetes credential provider plugin for multi-cloud cluster authentication (GCP/GKE, AWS/EKS, Azure/AKS). Dynamically generates authentication tokens using the ExecCredential API.
